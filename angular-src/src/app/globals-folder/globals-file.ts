// GLOBAL VARIABLES AND HELPERS

export const globals = {
   shouldMainContainerExpand: false
  };

export class LocalUserConnected {
  constructor(
    accessT: string,
    id: string,
    fullname: string,
    avatar: string
  ) {}
}
